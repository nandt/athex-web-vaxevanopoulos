<h2 class="titleContent heading3 weightBoldBook textColorDarkBlue">{{content.field_gs_title}}</h2>
<div class="my-slider">
  {% for item in paragraph.field_gs_image %}
    <div>
      <img src="{{ file_url(item.entity.fileuri) }}" alt="{{ item.alt }}" />
    </div>
  {% endfor %}
</div>



