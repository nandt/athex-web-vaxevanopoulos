{{ attach_library('athex/text_item') }}

<div class="container">
    <div class="row">
        <div class="col-12 col-lg-10">
            <div>{{ content.field_text_b }}</div>
            {% if content.field_text_cta is not empty %}
                <div class="exploreMore">{{ content.field_text_cta }}</div>
            {% endif %}
        </div>
    </div>
</div>
